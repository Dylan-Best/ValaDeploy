// Comportement de la page dashboard.
// Le tableau de projets est actuellement statique (données de démonstration dans le HTML).
// À compléter : remplacer le contenu de <tbody id="projects-table-body"> par un rendu
// dynamique à partir de l'API (voir scripts/api/projects.js à venir), en réutilisant
// les classes status-{created|building|running|stopped|error} définies dans dashboard.css.

document.addEventListener('DOMContentLoaded', () => {
    // TODO: fetchProjects() puis renderProjectsTable(data)
});
